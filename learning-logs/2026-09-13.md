# Daily Learning Log — 2026-09-13

## What I worked on
- No new feature code committed in the last 48 hours — only automated daily learning logs landed
- `app/api/webhooks/route.ts` and `app/api/cron/heartbeat/route.ts` remain the live infrastructure
- `app/page.tsx` still shows the default Next.js template — no UI work yet
- Yesterday's suggested `app/hooks/useHeartbeat.ts` has not yet been created

## Suggested next step

**Add webhook signature verification to `app/api/webhooks/route.ts` using HMAC-SHA256 — this is how you confirm that a request actually came from the service that sent it (GitHub, Stripe, etc.) and not a random third party.**

Today's focus: **Webhooks — signature verification (the security layer every real webhook needs)**

Right now your webhook route accepts any POST request. Real webhook providers sign their payloads with a secret so you can verify authenticity. Add this to `app/api/webhooks/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import crypto from "crypto";

export async function POST(req: NextRequest) {
  const secret = process.env.WEBHOOK_SECRET;
  if (!secret) return NextResponse.json({ error: "No secret configured" }, { status: 500 });

  const rawBody = await req.text();
  const signature = req.headers.get("x-hub-signature-256") ?? "";

  const expected = "sha256=" + crypto
    .createHmac("sha256", secret)
    .update(rawBody)
    .digest("hex");

  if (!crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expected))) {
    return NextResponse.json({ error: "Invalid signature" }, { status: 401 });
  }

  const payload = JSON.parse(rawBody);
  console.log("Verified webhook payload:", payload);
  return NextResponse.json({ ok: true });
}
```

Then add `WEBHOOK_SECRET` to your `.env.local` and Vercel environment variables. `timingSafeEqual` is crucial — it prevents timing attacks that a naïve `===` comparison would be vulnerable to.

---
*Generated by scheduled learning agent · beginloops*
